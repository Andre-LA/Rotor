return {
  bit_ids  = require 'motor.bit_ids',
  entities = require 'motor.entities',
  states   = require 'motor.states',
  storages = require 'motor.storages',
  systems  = require 'motor.systems'
}
